[core]
  editor = nvim
[commit]
  template = ~/.git_commit_tpl
  verbose = true
[filter "lfs"]
  required = true
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process

